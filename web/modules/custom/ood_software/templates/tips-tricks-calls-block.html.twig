{#
/**
 * @file
 * Default theme implementation for Tips & Tricks Calls block.
 *
 * Available variables:
 * - members: Array of member data with uid, name, and photo_url.
 */
#}

<style>
  .tips-tricks-block svg,
  .tips-tricks-block .profile-photo {
    height: 70px;
    width: auto;
  }
  .tips-tricks-block {
    max-height: 205px;
    overflow: auto;
  }
  .tips-tricks-block .rounded-custom {
    border-radius: 1rem 0 1rem 0;
  }
</style>
<div class="tips-tricks-block">
  {% if members %}
    <ul class="contributors-list d-flex flex-wrap list-none">
      {% for member in members %}
        <li class="contributor-item text-center mr-3 mb-3">
          <a href="/community-persona/{{ member.uid }}" class="contributor-link">
            <div class="contributor-photo">
              {% if member.photo_url %}
                <img src="{{ member.photo_url }}" alt="{{ member.name }}" class="profile-photo rounded-custom" />
              {% else %}
                <img src="modules/custom/access/modules/cssn/images/profile.gif" alt="{{ member.name }}" class="profile-photo rounded-custom" />
              {% endif %}
            </div>
            <div class="contributor-name">{{ member.name }}</div>
          </a>
        </li>
      {% endfor %}
    </ul>
  {% else %}
    <p>{{ 'No members found.'|t }}</p>
  {% endif %}
</div>
