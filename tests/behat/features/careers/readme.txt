Tests in this directory will be run on all domains

This allows testing a template test, without running all the template tests

The initial tag must be "@templates", as if it were in the templates directory.